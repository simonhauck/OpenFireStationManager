# OpenFireStationManager

OpenFireStationManager is an open-source monorepo for managing firefighting stations.

## Repository layout

- `client/` — React + TypeScript frontend
- `server/` — Spring Boot + Kotlin backend
