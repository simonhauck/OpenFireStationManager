services:
  app:
    image: ghcr.io/simonhauck/open-fire-station-manager:develop
    restart: unless-stopped
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_URL: ${DB_URL}
      REMEMBER_ME_KEY: ${REMEMBER_ME_KEY}
    ports:
      - "${APP_PORT}:8080"
